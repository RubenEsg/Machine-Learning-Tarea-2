# Dependencias de ejecución de la API (imagen python:3.10-slim y CI).
# Las versiones están fijadas a propósito: model.joblib solo se puede cargar
# de forma confiable con la MISMA versión de scikit-learn y numpy con la que
# se entrenó. Son las versiones más recientes con soporte para Python 3.10.
fastapi==0.141.1
uvicorn==0.52.4
scikit-learn==1.7.2
joblib==1.6.0
pydantic==2.13.5
pandas==2.3.3
numpy==2.2.6
scipy==1.15.3
